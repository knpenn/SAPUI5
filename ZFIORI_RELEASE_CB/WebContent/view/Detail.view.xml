<core:View
	controllerName="sap.ui.demo.ReleaseCB.view.Detail"
	xmlns="sap.m"
	xmlns:core="sap.ui.core" >
	<Page
		title="{i18n>DetailTitle}"
		showNavButton="true"
		navButtonPress="handleNavButtonPress" >
		<VBox>
			<Text text="{Vbeln}" />
			<Text text="{Vkorg}" />
			<Text text="{Vbtyp}" />
			<Text text="{Ernam}" />
		</VBox>
	</Page>
</core:View>